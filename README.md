# FNP

![ict](./imgs/ict.jpg)

## 架构


### 后端（FNP Daemon）
- 网络接口层：物理网卡、虚拟网卡（如TAP设备）等。统一抽象为网络接口，提供数据包的发送和接收功能。
    - id：唯一标识符
    - name：接口名称
    - type：接口类型（物理、tap）
    - MAC地址：只支持一个MAC地址
    - IP地址列表：支持多个IP地址
    - 默认网关：IP地址+出口设备ID

- 采用弱主机模型，允许接口接收目的地址为本机IP（可能是别的接口的IP）的数据包。